#include" main.h"
/**
 * _isdigit - function that checks fom 0 to 9
 * @c: The determinant factor
 * Return: 1 if c is a digit or 0 for anything else
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}
