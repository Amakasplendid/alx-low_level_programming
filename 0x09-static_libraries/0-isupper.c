#include "main.h"
/**
 * _isupper - lowecase character
 * @c: The determinant character
 * Return: 1 for uppercasw character or 0 for anything else
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
